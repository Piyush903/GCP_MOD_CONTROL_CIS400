{
  "index": "237c3c48-6825-437b-8f73-d8f891b69118",
  "description": "Google Cloud Platform (GCP) services write audit log entries to the Admin Activity and Data Access logs to help answer the questions of, \\",
  "modName": "gcp_compliance",
  "resourceName": "cis_v400_2_5",
  "severity": "low",
  "sql": "WITH filter_data AS (\n  SELECT \n    m.project AS project, \n    display_name AS alert_name, \n    COUNT(m.name) AS metric_name \n  FROM \n    gcp.gcp_monitoring_alert_policy as a,\n    jsonb_array_elements(conditions) AS filter_condition \n  JOIN \n    gcp.gcp_logging_metric m \n  ON \n    m.filter LIKE '%protoPayload.methodName:\"SetIamPolicy\"%'\n    AND m.filter LIKE '%protoPayload.serviceData.policyDelta.auditConfigDeltas:%'\n  AND \n    filter_condition -> 'conditionThreshold' ->> 'filter' LIKE '%metric.type = \"' || m.metric_descriptor_type || '\"%'\n  WHERE \n    enabled \n        and m.project_id=projectId\n        and m.cd_snapshot_version=snapshotVersion\n        and a.project_id=projectId\n        and a.cd_snapshot_version=snapshotVersion\n  GROUP BY \n    m.project, display_name, m.name\n) \nSELECT \n  'https://cloudresourcemanager.googleapis.com/v1/projects/' || project_id AS resource, \n  CASE \n    WHEN d.metric_name > 0 THEN 'ok' \n    ELSE 'alarm' \n  END AS status, \n  CASE \n    WHEN d.metric_name > 0 THEN 'Log metric and alert exist for audit configuration changes.' \n    ELSE 'Log metric and alert do not exist for audit configuration changes.' \n  END AS reason, \n  project_id AS project \nFROM \n  gcp.gcp_project AS p \nLEFT JOIN \n  filter_data AS d ON d.project = project\n          where p.project_id=projectId\n        and p.cd_snapshot_version=snapshotVersion;",
  "title": "2.5 Ensure That the Log Metric Filter and Alerts Exist for Audit Configuration Changes",
  "path": null,
  "qualifiedName": null,
  "query": "gcp_compliance.query.logging_metric_alert_audit_configuration_changes",
  "controlLevel": "master",
  "createdAt": [
    2025,
    11,
    23,
    23,
    55,
    51,
    711633000
  ],
  "remediationAvailable": false,
  "anonymous": false,
  "type": "Project",
  "isMaster": true,
  "__path": "/home/piyushnijhawan/Desktop/steampipe-mod-gcp-compliance/cis_v400/mod_control/cis_v400_2_5"
}