{
  "index": "d67bdb95-ce10-48a2-8af0-62b237304f73",
  "description": "It is recommended that Essential Contacts is configured to designate email addresses for Google Cloud services to notify of important technical or security information.",
  "modName": "gcp_compliance",
  "resourceName": "cis_v400_1_16",
  "severity": "medium",
  "sql": "with categories as ( select name, title, _ctx, organization_id, notificationtype from gcp.gcp_organization, jsonb_array_elements(essential_contacts) as ec, jsonb_array_elements_text(ec -> 'notificationCategorySubscriptions') as notificationtype where project_id=projectId and cd_snapshot_version=snapshotVersion ) select name resource, case when jsonb_array_length('[\"LEGAL\", \"SECURITY\", \"SUSPENSION\", \"TECHNICAL\", \"TECHNICAL_INCIDENTS\"]'::jsonb - array_agg(notificationtype)) = 0 then 'ok' when to_jsonb(array_agg(notificationtype)) @> '[\"ALL\"]'::jsonb then 'ok' else 'alarm' end as status, case when jsonb_array_length('[\"LEGAL\", \"SECURITY\", \"SUSPENSION\", \"TECHNICAL\", \"TECHNICAL_INCIDENTS\"]'::jsonb - array_agg(notificationtype)) = 0 then title || ' essential contacts are configured.' when to_jsonb(array_agg(notificationtype)) @> '[\"ALL\"]'::jsonb then title || ' essential contacts are configured.' else title || ' essential contacts are not configured.' end as reason, organization_id from categories group by name, title, _ctx, organization_id;",
  "title": "1.16 Ensure Essential Contacts is Configured for Organization",
  "path": null,
  "qualifiedName": null,
  "query": "gcp_compliance.query.organization_essential_contacts_configured",
  "controlLevel": "master",
  "createdAt": [
    2025,
    11,
    23,
    23,
    55,
    51,
    700050000
  ],
  "remediationAvailable": false,
  "anonymous": false,
  "type": "Project",
  "isMaster": true
}
