{
  "index": "682348f8-9e29-4fb1-8725-fcb0d8827f90",
  "description": "Run individual configuration, compliance and security controls or full compliance benchmarks for CIS, Forseti Security and CFT Scorecard across all your GCP projects.",
  "modName": "gcp_compliance",
  "resourceName": "cis_v130_6_2_6",
  "severity": "low",
  "sql": "select\n  self_link resource,\n  case\n    when database_version not like 'POSTGRES%' then 'skip'\n    when database_flags @> '[{\"name\":\"log_min_messages\",\"value\":\"on\"}]' then 'ok'\n    else 'alarm'\n  end as status,\n  case\n    when database_version not like 'POSTGRES%'\n      then title || ' not a PostgreSQL database.'\n    when database_flags is null or not (database_flags @> '[{\"name\":\"log_min_messages\"}]')\n      then title || ' ''log_min_messages'' database flag not set.'\n    when database_flags @> '[{\"name\":\"log_min_messages\",\"value\":\"on\"}]'\n      then title || ' ''log_min_messages'' database flag set to ''on''.'\n    else title || ' ''log_min_messages'' database flag set to ''off''.'\n  end as reason\n  \n  , location as location, project as project\nfrom\n gcp.gcp_sql_database_instance where project_id = projectId   and cd_snapshot_version = snapshotVersion;",
  "title": "Ensure that the 'log_min_messages' database flag for Cloud SQL PostgreSQL instance is set appropriately",
  "path": null,
  "qualifiedName": null,
  "query": "gcp_compliance.query.manual_control",
  "controlLevel": "master",
  "createdAt": [
    2023,
    7,
    5,
    12,
    0,
    35,
    477369000
  ],
  "remediationAvailable": false,
  "anonymous": false,
  "type": "SQL Database Instance"
}